# Pinned to the versions the test suite and the live pipeline runs were actually
# verified against. The previous pins had all drifted -- praw was pinned to
# 7.8.1 while 8.0.3 was installed, a major version apart -- so a deploy would
# have installed different code than anything that had been tested.
#
# rapidfuzz is the one package here that ships compiled wheels, and it is why
# the first Render build failed: 3.10.1 publishes wheels only up to cp313, so on
# Render's newer Python it fell back to a source build and died in
# scikit-build-core. 3.14.6 covers cp311-cp315. See also .python-version, which
# pins the interpreter so wheel availability stops being a moving target.
flask==3.1.3
gunicorn==26.2.0
requests==2.34.2
python-dotenv==1.2.2
praw==8.0.3
feedparser==6.0.14
rapidfuzz==3.14.6
supabase==2.31.0
